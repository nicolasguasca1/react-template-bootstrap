const OPERATIONS = {
    COST: 'Costo',
};

const TEST = {
    COST : {
        origin: "hospital", destination: "alcaldia",
        fee: 15000,
        result: 
};

describe("/api/calc", () => {
  const controller = "api/calc";
  describe("/cost", () => {
    const resource = "cost";
    const endpoint = `/${controller}/${resource}`;
  });
});
